name: flutter_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.2

  # QR Code Generator (kompatibel mit Flutter 2.x)
  qr_flutter: ^4.0.0

  # Firebase (kompatibel mit Flutter 2.x)
  firebase_core: ^2.4.1
  firebase_database: ^10.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
